ul class="nav nav-tabs"
  li = link_to tag_icon(:list, pat(:list)), url(:helper_requests, :index)
  li class="active" = link_to tag_icon(:plus, pat(:new)), url(:helper_requests, :new)

div class="tabs-content"
  = form_for :helper_request, url(:helper_requests, :create), :class => 'form-horizontal' do |f|
    = partial 'helper_requests/form', :locals => { :f => f }
